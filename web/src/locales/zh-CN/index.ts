import errors from 'locales/zh-CN/errors.json';
import tooltips from 'locales/zh-CN/tooltips.json';

// Join the translation JSON into different namespace.
const resource = {
  errors,
  tooltips,
};

export default resource;
