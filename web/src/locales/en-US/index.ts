import errors from 'locales/en-US/errors.json';
import tooltips from 'locales/en-US/tooltips.json';

// Join the translation JSON into different namespace.
const resource = {
  errors,
  tooltips,
};

export default resource;
