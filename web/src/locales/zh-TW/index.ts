import errors from 'locales/zh-TW/errors.json';
import tooltips from 'locales/zh-TW/tooltips.json';

// Join the translation JSON into different namespace.
const resource = {
  errors,
  tooltips,
};

export default resource;
