import errors from 'locales/zh-HK/errors.json';
import tooltips from 'locales/zh-HK/tooltips.json';

// Join the translation JSON into different namespace.
const resource = {
  errors,
  tooltips,
};

export default resource;
